<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar"/>
    <!-- Base application theme. -->
    <!-- activity_fullscreen -->
    <style name="activity_fullscreen" parent="AppTheme">
        <item name="android:background">@drawable/background_dark</item>
    </style>

    <style name="activity_fullscreen_somethings" parent="AppTheme">
        <item name="android:textColor">@color/fullactivityText</item>
        <item name="android:textColorHint">@color/fullactivityTextHint</item>
    </style>

    <style name="activity_fullscreen_button" parent="AppTheme">
        <item name="android:background">@drawable/button_layout</item>
    </style>




</resources>